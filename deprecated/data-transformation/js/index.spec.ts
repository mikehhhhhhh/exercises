const { transform } = require('./index');

describe('data transformation', () => {
  it.todo('Given the input string x, please count every occurrence of each alphabetic character, case insensitively.')
})