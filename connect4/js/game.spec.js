const { createGrid, doPlayerMove } = require("./game");

describe("connect 4", () => {
    it("should do something", () => {
        const grid = createGrid();

        console.log(grid);
    });
});
